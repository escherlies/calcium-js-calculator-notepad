// 🧮 Simple Math
const price = 25
const tax = price * 0.08
const total = price + tax
const out = `Price: $${price}, Tax: $${tax.toFixed(2)}, Total: $${total.toFixed(2)}`

// Your last statement will appear in the box below
out